import { kadenaDecrypt } from '../../index.js';
import { kadenaChangePassword as originalKadenaChangePassword } from '../kadena-crypto.js';
import { encryptLegacySecretKey } from './encryption.js';
export const kadenaChangePassword = async (secretKey, oldPassword, newPassword) => {
    const newSecretKey = await originalKadenaChangePassword(await kadenaDecrypt(oldPassword, secretKey), oldPassword, newPassword);
    return encryptLegacySecretKey(newPassword, newSecretKey);
};
//# sourceMappingURL=kadenaChangePassword.js.map