import { kadenaDecrypt } from '../../index.js';
import { kadenaSign as originalKadenaSign } from '../kadena-crypto.js';
export const kadenaSign = async (password, hash, // base64 message
secretKey) => {
    return await originalKadenaSign(password, Buffer.from(hash, 'base64'), await kadenaDecrypt(password, secretKey));
};
//# sourceMappingURL=kadenaSign.js.map